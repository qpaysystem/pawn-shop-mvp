// =============================================================================
// 1С: выгрузка структуры метаданных (таблицы, реквизиты) в один текстовый файл
// Для создания полноценного API по структуре базы.
//
// ЧТО ПОЛУЧИТЕ В ФАЙЛЕ (логические имена и типы):
//   Справочники: имя, синоним, реквизиты, табличные части.
//   Документы: имя, синоним, реквизиты, табличные части.
//   Регистры сведений: измерения, ресурсы.
//   Регистры накопления: измерения, ресурсы (для остатков — какие поля «склад», «номенклатура», «количество»).
//   Регистры бухгалтерии: измерения, ресурсы.
//
// КАК ПОЛУЧИТЬ СТРУКТУРУ ТАБЛИЦ В 1С (пошагово):
//
// 1. Откройте 1С в режиме КОНФИГУРАТОР (не 1С:Предприятие).
// 2. Подключитесь к нужной базе (например LMB).
// 3. Создайте внешнюю обработку: Файл → Новый → Внешняя обработка.
// 4. В открывшейся форме добавьте кнопку (например «Выгрузить») и в модуль обработки
//    скопируйте процедуру ВыгрузитьМетаданныеВФайл из этого файла (и Выполнить, если нужно).
// 5. Укажите путь к файлу в вызове: ВыгрузитьМетаданныеВФайл("C:/temp/1c_metadata.txt");
// 6. Запустите обработку (кнопка Выполнить или через Консоль кода).
// 7. Откройте полученный текстовый файл — в нём будут все объекты метаданных и их реквизиты.
//
// Сопоставление с физическими таблицами в PostgreSQL:
//   В файле — логические имена (Документ.СкупкаЦенностей, РегистрНакопления.ОстаткиТоваров).
//   В БД 1С — физические имена (_document389x1, _accumrg26227 и т.д.).
//   Связь «логическое имя → физическая таблица» по внутреннему ID; проще сопоставлять по количеству
//   реквизитов и по данным команды lmb:db-schema / lmb:1c-balances-discovery в нашем приложении.
//
// ВАЖНО: В языке запросов 1С (ВЫБРАТЬ ... ИЗ) нельзя получить метаданные —
// только данные. Поэтому используется встроенный язык и объект Метаданные.
// =============================================================================
//
// НЕ МЕНЯЙТЕ СТРОКУ НИЖЕ! Должно быть именно: (ПутьКФайлу) — имя параметра.
// Путь подставляете при ВЫЗОВЕ процедуры (кнопка, консоль): ВыгрузитьМетаданныеВФайл("C:/Users/evgeny/12.txt");
//
// =============================================================================

Процедура ВыгрузитьМетаданныеВФайл(ПутьКФайлу) Экспорт
    // ПутьКФайлу — полный путь, например "C:\temp\1c_metadata.txt"
    Запись = Новый ЗаписьТекста(ПутьКФайлу, КодировкаТекста.UTF8);
    МД = Метаданные;

    Запись.ЗаписатьСтроку("=== СПРАВОЧНИКИ ===");
    Для Каждого Объект Из МД.Справочники Цикл
        Запись.ЗаписатьСтроку("");
        Запись.ЗаписатьСтроку("Справочник." + Объект.Имя + "  // " + Объект.Синоним);
        Для Каждого Рекв Из Объект.Реквизиты Цикл
            Запись.ЗаписатьСтроку("  Реквизит: " + Рекв.Имя + " (" + Строка(Рекв.Тип) + ")");
        КонецЦикла;
        Для Каждого ТЧ Из Объект.ТабличныеЧасти Цикл
            Запись.ЗаписатьСтроку("  ТабличнаяЧасть: " + ТЧ.Имя);
            Для Каждого КолонкаТЧ Из ТЧ.Реквизиты Цикл
                Запись.ЗаписатьСтроку("    " + КолонкаТЧ.Имя + " (" + Строка(КолонкаТЧ.Тип) + ")");
            КонецЦикла;
        КонецЦикла;
    КонецЦикла;

    Запись.ЗаписатьСтроку("");
    Запись.ЗаписатьСтроку("=== ДОКУМЕНТЫ ===");
    Для Каждого Объект Из МД.Документы Цикл
        Запись.ЗаписатьСтроку("");
        Запись.ЗаписатьСтроку("Документ." + Объект.Имя + "  // " + Объект.Синоним);
        Для Каждого Рекв Из Объект.Реквизиты Цикл
            Запись.ЗаписатьСтроку("  Реквизит: " + Рекв.Имя + " (" + Строка(Рекв.Тип) + ")");
        КонецЦикла;
        Для Каждого ТЧ Из Объект.ТабличныеЧасти Цикл
            Запись.ЗаписатьСтроку("  ТабличнаяЧасть: " + ТЧ.Имя);
            Для Каждого КолонкаТЧ Из ТЧ.Реквизиты Цикл
                Запись.ЗаписатьСтроку("    " + КолонкаТЧ.Имя + " (" + Строка(КолонкаТЧ.Тип) + ")");
            КонецЦикла;
        КонецЦикла;
    КонецЦикла;

    Запись.ЗаписатьСтроку("");
    Запись.ЗаписатьСтроку("=== РЕГИСТРЫ СВЕДЕНИЙ ===");
    Для Каждого Объект Из МД.РегистрыСведений Цикл
        Запись.ЗаписатьСтроку("");
        Запись.ЗаписатьСтроку("РегистрСведений." + Объект.Имя + "  // " + Объект.Синоним);
        Для Каждого Рекв Из Объект.Измерения Цикл
            Запись.ЗаписатьСтроку("  Измерение: " + Рекв.Имя + " (" + Строка(Рекв.Тип) + ")");
        КонецЦикла;
        Для Каждого Рекв Из Объект.Ресурсы Цикл
            Запись.ЗаписатьСтроку("  Ресурс: " + Рекв.Имя + " (" + Строка(Рекв.Тип) + ")");
        КонецЦикла;
    КонецЦикла;

    Запись.ЗаписатьСтроку("");
    Запись.ЗаписатьСтроку("=== РЕГИСТРЫ НАКОПЛЕНИЯ ===");
    Для Каждого Объект Из МД.РегистрыНакопления Цикл
        Запись.ЗаписатьСтроку("");
        Запись.ЗаписатьСтроку("РегистрНакопления." + Объект.Имя + "  // " + Объект.Синоним);
        Для Каждого Рекв Из Объект.Измерения Цикл
            Запись.ЗаписатьСтроку("  Измерение: " + Рекв.Имя + " (" + Строка(Рекв.Тип) + ")");
        КонецЦикла;
        Для Каждого Рекв Из Объект.Ресурсы Цикл
            Запись.ЗаписатьСтроку("  Ресурс: " + Рекв.Имя + " (" + Строка(Рекв.Тип) + ")");
        КонецЦикла;
    КонецЦикла;

    Запись.ЗаписатьСтроку("");
    Запись.ЗаписатьСтроку("=== РЕГИСТРЫ БУХГАЛТЕРИИ ===");
    Для Каждого Объект Из МД.РегистрыБухгалтерии Цикл
        Запись.ЗаписатьСтроку("");
        Запись.ЗаписатьСтроку("РегистрБухгалтерии." + Объект.Имя + "  // " + Объект.Синоним);
        Для Каждого Рекв Из Объект.Измерения Цикл
            Запись.ЗаписатьСтроку("  Измерение: " + Рекв.Имя + " (" + Строка(Рекв.Тип) + ")");
        КонецЦикла;
        Для Каждого Рекв Из Объект.Ресурсы Цикл
            Запись.ЗаписатьСтроку("  Ресурс: " + Рекв.Имя + " (" + Строка(Рекв.Тип) + ")");
        КонецЦикла;
    КонецЦикла;

    Запись.Закрыть();
    Сообщить("Метаданные выгружены в файл: " + ПутьКФайлу);
КонецПроцедуры

// -----------------------------------------------------------------------------
// ПРОЦЕДУРА БЕЗ ПАРАМЕТРОВ: назначьте её на кнопку «Выполнить» или запуск обработки.
// Путь к файлу задан внутри — при необходимости измените его здесь (строка ниже).
// -----------------------------------------------------------------------------

Процедура Выполнить(Команда)
    ВыгрузитьМетаданныеВФайл("C:/Users/evgeny/12.txt");
КонецПроцедуры

// Альтернатива: если у обработки кнопка не «Выполнить», а своя — вызовите:
//   ВыгрузитьМетаданныеВФайл("C:/Users/evgeny/12.txt");
