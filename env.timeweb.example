# Образец .env для хостинга Timeweb
# Скопировать в .env на сервере и подставить APP_URL и пароль БД из панели.
# Команда на сервере: cp .env.example .env && php artisan key:generate
# Затем заменить блок APP_ и DB_ на содержимое ниже (вписать пароль и домен).

APP_NAME="Ломбард MVP"
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=https://ваш-домен.ru
APP_TIMEZONE=Europe/Moscow
APP_LOCALE=ru

DB_CONNECTION=mysql
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=cf89938_pawnshopmvp
DB_USERNAME=cf89938
DB_PASSWORD=ПАРОЛЬ_ИЗ_ПАНЕЛИ_TIMEWEB

SESSION_DRIVER=file
SESSION_LIFETIME=120

LOG_CHANNEL=stack
LOG_LEVEL=error

CACHE_STORE=file
QUEUE_CONNECTION=sync

# Лендинг ломбарда
LOMBARD_NAME="Ломбард"
LOMBARD_PHONE="+7 (383) 291-00-51"
